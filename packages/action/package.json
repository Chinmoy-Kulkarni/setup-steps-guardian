{
  "name": "@setup-fleet/action",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@setup-fleet/contracts": "workspace:*",
    "@setup-fleet/policy-engine": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  }
}
